app:
  env: "development"

logging:
  level: "debug"
  format: "text"
  add_source: true

pipeline:
  workers: 2
  queue_size: 64
  batch_size: 1

storage:
  raw:
    path: "./var/development/raw"
  metadata:
    backend: "memory"
  vector:
    backend: "memory"
